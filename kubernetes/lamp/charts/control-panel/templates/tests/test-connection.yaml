apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "control-panel.fullname" . }}-test-connection"
  labels:
    {{- include "control-panel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "control-panel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
