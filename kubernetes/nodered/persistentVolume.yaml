kind: PersistentVolume
apiVersion: v1
metadata:
  name: nodered-volume
  labels:
    #type: local
    app: nodered
spec:
  storageClassName: local-storage
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/volumes/nodered"