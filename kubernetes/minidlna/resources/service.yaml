apiVersion: v1
kind: Service
metadata:
  name: minidlna-pod
  labels:
    rio/iotplatform: minidlna
spec:
  ports:
  - name: http
    protocol: TCP 
    port: 8200
  #- name: https #TODO: wich port must expose minidlna??
  #  protocol: UDP
  #  port: ???
  selector:
    matchLabels:
      app: minidlna-pod
  type: NodePort 
